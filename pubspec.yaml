name: timetrackerflutter
description: Time Tracker Project

version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  firebase_auth: ^0.16.0
  google_sign_in: ^4.4.1
  flutter_facebook_login: ^3.0.0
  provider: ^4.0.5
  cloud_firestore: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
  - assets/images/
